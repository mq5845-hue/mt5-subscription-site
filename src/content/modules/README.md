# Module Content Drop Zone

請將各模組的 Markdown 原文放到這個資料夾中，檔名需與 `src/lib/module-library.js` 的 `slug` 一致。

例如：
- `module-00-base-config.md`
- `module-01-new-bar-detector.md`
- `module-02-money-management.md`

目前頁面會自動讀取這裡的 `.md` 檔，並把內容拆成：
- 上半部完整原文
- 下半部毛玻璃半開放預覽

如果對應檔案不存在，頁面會顯示提醒訊息，不影響整體網站運作。
